#!/bin/sh
# Kept for configurations written against the old path. The launcher now
# lives with the Claude Code plugin so a marketplace copy is self-contained.
exec sh "$(cd -- "$(dirname -- "$0")" && pwd)/claude-code/hooks/pyxray-hook"
